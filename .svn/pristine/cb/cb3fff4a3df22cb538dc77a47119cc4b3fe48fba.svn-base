import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TwoWheelerPremiumComponent } from './twoWheeler-premium.component';

describe('CarPremiumComponent', () => {
  let component: TwoWheelerPremiumComponent;
  let fixture: ComponentFixture<TwoWheelerPremiumComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TwoWheelerPremiumComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TwoWheelerPremiumComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
