import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CarInsurerDetailsComponent } from './car-insurer-details.component';

describe('CarInsurerDetailsComponent', () => {
  let component: CarInsurerDetailsComponent;
  let fixture: ComponentFixture<CarInsurerDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CarInsurerDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CarInsurerDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
